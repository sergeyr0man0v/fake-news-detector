defaults:
  - _self_
  - data: default
  - module: distilbert
  - trainer: default
  - callbacks: default
  - logging: mlflow

hydra:
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}
